# CFG - A CFG Parser in Zig

This is a CFG parser generated by Bison, then used from within a Zig program.

## Grammar

```
Expr   -> Term (("+" | "-") Term)*
Term   -> Factor (("*" | "/") Factor)*
Factor -> "(" Expr ")" | Number
Number -> [0-9]+
```
